using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Prefex : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("EU SOU UM EXEMPLO");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
